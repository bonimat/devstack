services:
  db:
    build: ./db/oracle-xe 
    container_name: ${DEVSTACK_PREF_NAME:-ds}_oracle
    ports:
      - 32118:1521
      - 35518:5500
    volumes:
      - $DEVSTACK_ORACLE_DATA:/opt/oracle/oradata